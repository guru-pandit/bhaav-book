<?xml version="1.0" encoding="utf-8"?>
<!--
    Only the one subdirectory CsvExporter writes share files into. Exposing the
    whole cache directory would hand every receiving app a grant over anything
    else the app has cached.
-->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
